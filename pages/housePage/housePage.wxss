/* pages/tem.wxss */
.root {
  margin-top: 100rpx;
  padding-bottom: 50rpx;
}

.btn_group {
  display: flex;
  justify-content: center;
}

.input_view {
  position: absolute;
  top: 0;
  width: 100%;
  height: 100%;
  backdrop-filter: blur(3px);
}

.input_group {
  position: relative;
  margin-left: 60rpx;
}

.hide {
  display: none;
}